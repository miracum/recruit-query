omop:
  jdbcUrl: "jdbc:postgresql://localhost:25432/ohdsi"
  username: "postgres"
  password: "postgres"

query:
  schedule:
    enable: true
    unixCron: "* * * * *" # run every minute
  webapi:
    auth:
      enabled: false
      oauth:
        token-url: "http://localhost:8085/auth/realms/MIRACUM/protocol/openid-connect/token"
        client-id: "recruit-query-user"
        client-secret: "a311cfd8-81bf-499b-b2b4-b5b4e0fffe24"
atlas:
  url: "http://localhost:8083/WebAPI"

fhir:
  url: "http://localhost:8082/fhir"

# to configure logging levels
logging:
  level:
    org:
      springframework: "INFO"
      apache:
        camel:
          spring:
            boot: "INFO"
          impl: "DEBUG"
      miracum:
        recruit:
          query: "DEBUG"

opentracing:
  jaeger:
    enabled: true
    log-spans: true
